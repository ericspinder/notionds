package com.notionds.dataSource.connection.logging;

import com.notionds.dataSource.Options;

import java.util.UUID;

public class ResultSetLogging<O extends Options> extends DbObjectLogging<O> {


    public ResultSetLogging(O options, UUID connectionId) {
        super(options, connectionId);
    }
    public
}
